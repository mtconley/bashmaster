#!/usr/bin/env bash
load_file() { . $@; }
load_dir() { for i in `ls -ap $@ | grep -v /`; do load_file "$@/$i"; done }

# Load bash export files
load_dir $BASH_DIR/configs

# Load bash script files
load_dir $BASH_DIR/scripts
